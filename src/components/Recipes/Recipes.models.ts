export interface IRecipe {
    json: string
    name: string
    url: string
    id: string
    images: string[]
    category: string
    cuisine: string
    ingredients: any[]
    yield: string
    cookTime: string
    prepTime: string
    notes:string
}